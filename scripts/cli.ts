#!/usr/bin/env npx tsx

/**
 * Harness CLI
 *
 * Commands:
 *   init    - Add git submodules defined in meta.ts
 *   sync    - Update submodules and copy vendor skills to skills/
 *   check   - Report which submodules have upstream updates
 *   cleanup - Remove submodules and skills not in meta.ts
 *
 * Usage:
 *   pnpm start
 *   pnpm start -- -y  (non-interactive)
 */

// TODO: Implement CLI commands
console.log('harness cli — not yet implemented')
